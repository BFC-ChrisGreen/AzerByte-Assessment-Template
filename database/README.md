# Database

The Azerbyte SQLite database and any database setup scripts will be stored here.

Database files are excluded from Git. The repository must contain enough setup code or instructions to recreate the required database using fictional data.

