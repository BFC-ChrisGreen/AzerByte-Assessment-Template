# Azerbyte Server

This folder will contain the Flask API and server-side code.

During Topic 4, you will create `server.py` and build the first API endpoint one part at a time. Character endpoints and persistent storage will follow in later stages.

Do not create `server.py` until you reach that stage in `guides/02-your-first-api.md`.
